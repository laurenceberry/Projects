%section.introduction
  .section-inner
    %h1
      - if type == "index"
        =t('basic.short_bio.intro', :href => link_to(t("basic.short_bio.gds_href"), 'http://www.gov.uk/')).html_safe
      - elsif type == "scrapbook"
        Scrapbook
      - elsif type == "weeknotes"
        Weeknotes
    - if type == "index"
      =link_to "Read more", "/about", class: "desktophidden"
